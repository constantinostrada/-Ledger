export interface AccountDTO {
  id: string;
  userId: string;
  name: string;
  type: string;
  balanceCents: number;
  currency: string;
  isActive: boolean;
  createdAt: string;
  updatedAt: string;
}
