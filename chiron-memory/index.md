# Project memory

This folder holds the project's canonical decisions, architecture and conventions as markdown — generated by Chiron from distilled memory (source of truth is the memory store). Read this map first; the long tail lives in RAG (memory_search).

## Sections
- [[architecture/account-now-has-a-required-fk-to-user-account-us]] — Account now has a required FK to User (Account.userId) with cascade delete, migrated via `add_users` Prisma migration
- [[architecture/authenticaterequest-is-the-sole-place-that-extra]] — authenticateRequest() is the sole place that extracts/verifies the Bearer JWT and resolves request identity; accounts/transactions routes 401 without a valid token and derive all scoping from it
- [[architecture/category-is-a-new-domain-entity-added-in-b1-not]] — Category is a new domain entity added in B1 (not in the original boilerplate), with its own ICategoryRepository port and PrismaCategoryRepository implementation alongside the pre-existing Account/Transaction ports; Transaction now carries an optiona…
- [[architecture/jwttokenservice-requires-the-jwt-secret-to-be-pa]] — JwtTokenService requires the JWT secret to be passed at construction time (fails fast if missing) rather than reading it lazily per-call
- [[convention/auth-routes-use-201-on-successful-register-409-o]] — Auth routes use 201 on successful register, 409 on duplicate email, 200 on successful login, 401 on any login failure (unknown email or wrong password alike)
- [[convention/bcryptpasswordhasher-uses-10-salt-rounds-jwttoke]] — BcryptPasswordHasher uses 10 salt rounds; JwtTokenService issues tokens with 1-day expiry
- [[convention/before-declaring-an-auth-authorization-feature-c]] — Before declaring an auth/authorization feature complete, verification is done by starting the real dev server and running a curl-based e2e script that checks token issuance, spoofed-userId rejection, missing/garbage-token 401s, and cross-user data i…
- [[convention/domain-entities-account-transaction-category-use]] — Domain entities (Account, Transaction, Category) use a private constructor plus two static factories: `create()` for brand-new entities (sets timestamps/defaults) and `reconstitute()` for rehydrating from persistence.
- [[convention/money-value-object-stores-only-integer-cents-con]] — Money value object stores only integer cents; constructor is private and callers must use `Money.fromCents(cents)` (plus `Money.zero()`), which throws via `Number.isSafeInteger` on floats, NaN, and Infinity.
- [[convention/new-entities-user-follow-the-same-private-constr]] — New entities (User) follow the same private-constructor + create()/reconstitute() factory pattern as existing domain entities, performing validation and normalization (lowercasing email, checking format/length) inside the factory rather than at the…
- [[convention/prisma-schema-stores-all-monetary-fields-as-int]] — Prisma schema stores all monetary fields as `Int` cents columns (e.g. balance_cents, amount_cents), never Decimal/Float; DTOs mirror this with explicit `*Cents` field names (balanceCents, amountCents) instead of generic amount/balance, and zod valid…
- [[convention/prisma-seed-ts-is-idempotent-by-using-determinis]] — prisma/seed.ts is idempotent by using deterministic ids plus upserts.
- [[convention/registeruserusecase-looks-up-the-email-in-iuserr]] — RegisterUserUseCase looks up the email in IUserRepository first and returns a duplicate/409 error before hashing the password or saving
- [[convention/transaction-entity-now-enforces-a-positive-amoun]] — Transaction entity now enforces a positive-amount invariant — `Transaction.create()`/`reconstitute()` throws if the amount is not positive — in addition to the new optional `categoryId`.
- [[convention/userid-is-never-accepted-from-client-input-dtos]] — userId is never accepted from client input (DTOs/body/query) for scoped resources — it's always the authenticated identity passed as an explicit first argument to use cases
- [[convention/when-a-resource-account-transaction-doesn-t-belo]] — When a resource (account/transaction) doesn't belong to the requesting user, use cases return "Account not found" rather than a 403/forbidden
- [[decision/loginuserusecase-returns-the-identical-invalid-c]] — LoginUserUseCase returns the identical "Invalid credentials" error whether the email doesn't exist or the password is wrong
- [[decision/replaced-the-boilerplate-s-raw-pg-postgres-drive]] — Replaced the boilerplate's raw `pg` Postgres driver stack (connection pool, hand-written SQL migration files, Postgres*Repository classes, scripts/migrate.ts) with Prisma (schema.prisma + prisma migrate + PrismaClient singleton + Prisma*Repository c…
